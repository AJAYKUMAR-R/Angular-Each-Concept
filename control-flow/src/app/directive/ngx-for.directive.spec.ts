import { NgxForDirective } from './ngx-for.directive';

describe('NgxForDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxForDirective();
    expect(directive).toBeTruthy();
  });
});
