import { TableHighlighterDirective } from './table-highlighter.directive';

describe('TableHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new TableHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
